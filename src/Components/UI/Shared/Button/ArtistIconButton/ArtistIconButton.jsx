import React from 'react';

import classes from './ArtistIconButton.module.css';

const ArtistIconButton = (props) => {
  return <i className={classes.artistButtonBefore}></i>;
};

export default ArtistIconButton;
