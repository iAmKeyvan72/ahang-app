import React from 'react';

import classes from './AddToPlaylistIconButton.module.css';

const AddToPlaylistIconButton = (props) => {
  return <i className={classes.addToPlaylistButtonBefore}></i>;
};

export default AddToPlaylistIconButton;
