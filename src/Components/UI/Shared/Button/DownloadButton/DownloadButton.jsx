import React from 'react';

import classes from './DownloadButton.module.css';

const DownloadButton = (props) => {
  return <i className={classes.notDownloaded}></i>;
};

export default DownloadButton;
