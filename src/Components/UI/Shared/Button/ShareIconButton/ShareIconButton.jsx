import React from 'react';

import classes from './ShareIconButton.module.css';

const ShareIconButton = (props) => {
  return <i className={classes.shareButtonBefore}></i>;
};

export default ShareIconButton;
