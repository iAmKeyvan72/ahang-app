import React from 'react';

import classes from './VideoIconButton.module.css';

const VideoIconButton = (props) => {
  return <i className={classes.videoButtonBefore}></i>;
};

export default VideoIconButton;
